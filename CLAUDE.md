@AGENTS.md

# 메이플랜드 주문서 강화 분석기

목표 공격력까지의 최소 기대비용 전략과 손절 시점을 계산하는 정적 웹앱.
계산은 전부 브라우저에서 끝나고 서버가 없다.

```bash
npm run dev        # 개발 서버
npm test           # 엔진 검증 80건
npm run typecheck  # next typegen + tsc  (typegen 먼저 돌아야 라우트 타입이 생긴다)
npm run lint
npm run build      # 정적 내보내기 → out/
npm run report     # 예제 시나리오 전체 분석을 터미널에 출력 (숫자 눈으로 보기)
npm run diag       # 입력 시세의 정합성(매물 이론가) 확인
```

## 깨뜨리면 안 되는 것

### 1. 라이선스 경계를 흐리지 않는다

코드는 MIT 다. 예외는 **게임 아이콘 하나뿐**이고, 그 예외는 좁게 유지한다.

- `public/game-icons/` 와 `src/app/icon.png` 는 넥슨 저작물이라 MIT 적용 대상이 아니다.
  게임과 같은 아이콘이라야 이용자가 주문서를 헷갈리지 않아서 넣었고, LICENSE 와 README
  양쪽에 범위를 적어 뒀다. 이 예외를 다른 디렉터리로 넓히지 말 것.
- 게임 에셋을 새로 추가할 일이 생기면 `scripts/fetch-game-icons.mjs` 에 출처를 남기고
  `public/game-icons/` 안에만 둔다. 출처가 코드에 없으면 나중에 경계가 사라진다.
- Galmuri 글꼴은 CDN 링크만 한다. 셀프 호스팅으로 바꾸면 OFL 사본을 동봉해야 한다.

### 2. 시세 모델의 정합성

이 엔진에서 가장 자주 깨지는 지점이다. 자세한 배경은 README 의 "남은 업횟의 가치는 묻지
않고 유도한다" 참고.

- **남은 업횟의 가치를 사용자에게 묻지 않는다.** `W(0,a) = V(a)`, `W(u,a) = max_s[E W(u−1,·) − c_s]`
  로 유도한다. "1회당 N메소" 같은 상수를 입력받으면 시세 곡선과 모순이 나고, 모델 안에
  차익거래가 생겨 최소비용이 −∞ 로 발산한다.
- **업횟이 남은 매물은 못 판다**는 전제다(거래가 거의 없다). 손절하려면 남은 업횟을 다
  태워 완작으로 만들어야 한다. 그래서 남은 업횟이 부채가 될 수 있고, `W(u,a) < V(a)` 가
  정상이다. 완작 시세를 하나도 안 채우면 되팔기 자체가 꺼진다(리버스처럼 교환불가인 경우).
- 테스트 픽스처나 예제의 시세를 손댈 때는 `npm run diag` 로 **매물가 ≥ 이론가**인지 확인할 것.
  이론가보다 싼 매물을 적으면 "사서 강화만 해도 이득"이 되어 해가 정의되지 않는다.
- 목표 공격력의 완작 시세를 채우면 답은 "그냥 사라"로 자명해진다. 예제에서 목표 시세를
  비워 둔 건 실수가 아니다.

### 3. 리버스 무기는 "레벨업 먼저" 모델이다

`Problem.startBonus` 는 격자 진입점의 분포다. 레벨업이 공짜(메소 기준)이고 정보를 늘려
주므로 강화 전에 다 굴리는 게 손해가 아니라는 전제에서 나온다. 진입점은 네 곳
(`dp-cost` 의 `startValue`, `dp-budget` 의 `valueAfterBuying`, `evaluate` 의 `afterBuying` 과
초기 질량)이고, 하나라도 빠뜨리면 몬테카를로 교차검증이 깨진다.

리버스를 켜면 매물 이론가가 레벨업 기댓값만큼 올라가므로, 일반 무기 시세를 그대로 두면
"이론가보다 싼 매물" 경고로 빠진다. 버그가 아니라 정상 동작이다.

### 4. 차트 색을 바꾸면 검증기를 다시 돌린다

정책 격자의 계열색 4종은 dataviz 스킬의 `validate_palette.js` all-pairs 기준을 통과한
조합이다(dark, surface `#1a1d21`). 색맹 분리가 6–8 경고대라 **칸마다 문자 라벨이 필수**이고,
빨강+주황 조합은 정상 시력 기준으로도 하드 실패라 쓸 수 없다.

### 5. 서버 코드를 추가하지 않는다

GitHub Pages 정적 호스팅이다. 라우트 핸들러·서버 액션·미들웨어를 넣으면 `next build` 의
`output: 'export'` 에서 깨진다. `basePath` 는 CI 가 `NEXT_PUBLIC_BASE_PATH` 로 주입한다.

## 구조

| 경로 | 역할 |
| --- | --- |
| `src/lib/enhance/dp-cost.ts` | 모드 A — 최소 기대비용. 재시작 고정점은 `R ← f(R)` 단조 반복 + Aitken 가속 (정책 반복은 부적절한 정책에 빠져 발산한다) |
| `src/lib/enhance/dp-budget.ts` | 모드 C — 예산 제약 하 달성 확률 |
| `src/lib/enhance/evaluate.ts` | 정책 고정 후 비용 CDF·분위수, 결과 분포 |
| `src/lib/enhance/salvage.ts` | 시세 보간과 상태 이론가 `W` |
| `src/components/` | UI. `PolicyHeatmap` 이 시그니처 화면 |
| `scripts/` | 리포트·정합성 진단·아이콘 생성 |

## 테스트

엔진은 시뮬레이션이 아니라 엄밀해라서, 테스트도 "돌아가나" 가 아니라 **값이 맞나**를 본다.

- 닫힌 형태와 대조 (100% 만 쓰면 결정론적, 단발 승부는 `(매물가+주문서가)/성공률`)
- 정책을 그대로 20만 회 굴려 기대비용이 DP 값의 4σ 안에 드는지
- 단조성 불변식 (목표↑ 비용↑, 업횟↑ 비용↓, 예산↑ 확률↑)
- 두 DP 상호 검증 (예산 최적 정책은 비용 최적 정책보다 달성 확률이 낮을 수 없다)

엔진을 고쳤는데 이 중 하나가 깨지면 대개 고친 쪽이 틀린 것이다.
